name: Run tests

on:
  push:
    branches:
      - v6
  pull_request:
    branches:
      - v6

jobs:
  unit:
    name: go test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.24"
          cache: true
      - run: go test -v -cover ./...
